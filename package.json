{
  "name": "biskuvi",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "dev": "bun build ./src/main.ts --outdir ./scripts --watch",
    "clean": "mkdir -p ./pack && rm -r ./pack/* && mkdir -p ./pack/biskuvi",
    "pack": "bun run clean && cp -r ./manifest.json ./scripts/ ./images/ ./pack/biskuvi"
  },
  "devDependencies": {
    "@types/bun": "^1.1.12"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
